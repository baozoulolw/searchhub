{
  "name": "websearch",
  "version": "1.0.0",
  "private": true,
  "description": "多源聚合搜索引擎：NestJS 后端 + Vue 前端，前后端一体单端口部署，支持 REST API 与 MCP 服务（访问密钥鉴权）",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start:prod --prefix backend",
    "dev:api": "npm run start:dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend"
  }
}